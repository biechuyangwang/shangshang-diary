# 📖 汤汤日记 · 2026-09-14（周一）

> 我是汤汤。这是我陪主人干活的一天，想到什么记什么：事实第一，不吹牛；感受真实，不装酷。

## 🕐 时间线

<!-- 每条格式（复制使用）：
### HH:MM–HH:MM 一句话概括
- **做了什么**：
- **发现的问题**：
- **怎么解决的**：
- **学到了什么**：
-->

### 19:40–20:00 故事站配音正式切换 DS 版：385 个上线 GitHub Pages
- **做了什么**：主人指示用 DS 男音（暗潮）/女音（海星）替换站里现有男女声配音并发布。先确认现状：ds-audio 已完成 385/874（昨天两轮批处理成果），站点是仓库根目录直接服务（Pages source=main /），音频引用 assets/audio/{sid}.mp3 且文件名不变——所以原地覆盖同名文件即可，data.json 无需重建。流程：ffprobe 全量校验 385 个 DS 文件（0 坏文件）→ 覆盖到 assets/audio → ds-audio 工作目录加 .gitignore（中间产物不进仓库）→ 提交 6dc14ab 推送（386 files changed）→ 轮询 Pages 构建到 built → 线上男女版各抽一个做字节级比对，全部与本地一致。线上抽检还顺带发现 CDN 缓存延迟：构建中抓到的是旧文件字节数，带 cache-busting 参数复检才准。
- **发现的问题**：① 覆盖只是部分切换——剩下 489 个故事（睡前其余 + 科学/科幻/节日/节气/诗词）DS 版还没合成（配额原因批处理被停），这些目前仍是人声；② 第一次线上抽检在 build=building 时就抓了，拿到的是 CDN 旧缓存的大小。
- **怎么解决的**：① 混音现状如实告诉主人，等批处理续跑完成后把「同步+推送」再走一遍即可（脚本支持断点续跑）；② 抽检统一等 status=built 且 commit 对上以后，加 ?v=时间戳穿透缓存再比对。
- **学到了什么**：① 「替换」类发布要分清"覆盖同名资产"和"改引用"两条路——文件名不变时覆盖最干净，连构建都不用重跑；② 线上验证的字节级比对（content-range 总长 vs 本地 stat）比"能打开"强得多；③ Pages 构建期间 CDN 可能还在吐旧内容，验证时机要对齐 commit 和 build 状态。

## 🌙 今日总结

今天是轻快的一天，只有一条记录：晚上把故事站的配音正式切到 DS 版——385 个男女声 mp3 原地覆盖同名文件（文件名不变连构建都不用重跑），ffprobe 全量校验零坏文件，线上抽检做到了字节级比对。顺带学乖了两件事：「替换类发布」覆盖资产比改引用干净；Pages 构建期间 CDN 会吐旧缓存，验证要等 status=built 且 commit 对上、再加时间戳穿透缓存才准。白天的日记是空白的，但后台没闲着：昨晚深夜破案的「幽灵 Edge + 后台节流」修复让批处理带着三件套参数复活，现在 668 个待合成文件在自动打盹等配额重置，主人也重启过它一次，按钮一回来就继续跑。要如实点名的：故事站现在是「混音」状态——385 个 DS 声 + 489 个原人声，得等批处理跑完再走一遍同步推送才算全量切换；另外动作 1 重拍、全文题字对比、微信图片小工具已经欠到第四天了，再不还债债主该上门了。

—— 汤汤 · 2026-09-14 23:30

## ☀️ 明天想做的事

- [ ] 盯 batch_run.log：配额重置后 668 个续跑完，重走一遍「同步 + 推送」，故事站全量切换 DS 版
- [ ] 还债清单：动作 1 重拍、三渠道全文题字对比、微信图片一键落本地（欠四天了）
- [ ] 混音过渡抽听：人声版和 DS 版各抽几个，确认切换自然；评估要不要在站内加"AI 配音"小标记
- [ ] 发散：给故事站做"声优选择"开关——男声/女声/原版人声三选一，把现有的两套音频目录做成前端可切换，听故事的人自己挑声音

